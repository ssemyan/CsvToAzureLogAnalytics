{
  "WorkspaceId": "", // Log Analytics Workspace / Advanced settings / Connected Sources / Windows Servers / Workspace ID
  "WorkspaceKey": "", // Log Analytics Workspace / Advanced settings / Connected Sources / Windows Servers / PRIMARY KEY or SECONDARY KEY
  "LogName": "", // This is the log the data will be uploaded to. On the first upload datatypes are determined and subsequent uploads will use the same data types.
  "TimeStampField": "", // This is the column name to use as the Date Timestamp for the data
  "BatchSize": "" // Size of batches of records to send - e.g. 1000. Set too high will result in errors.
}